import promptSync from 'prompt-sync';

/*
const message: string = "hello world";
console.log(message);

console.log(`this is the message ${message}`);

/*
number: For numeric values.
string: For textual data.
boolean: For true/false values.
any: A wildcard type that can be anything.
arrays: For collections of values, denoted by Type[] or Array<Type>.
*/

/*
const num:number = 18;

let name12:string = "hassan";

let check:boolean = true ;

let car:any = false ;
*/
/*
let cars:string[] = ["gadi","landcruiser"];

console.log(num);
console.log(name12);
console.log(check);
console.log(car);

// Use a for...of loop to iterate over the values of the array, not the indices.
for(const car of cars){
    console.log(car);
}
*/




function ageFinder(age : number) : number{
    if(age<0){
        console.log
    }
}