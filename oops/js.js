/*
class Car {
  constructor(brand, model) {
    this.brand = brand;
    this.model = model;
  }

  displayCar() {
    return `${this.brand} ${this.model}`;
  }
}

const toyota = new Car("corolla", "2015");
console.log(toyota.displayCar());
*/

class Car{
    constructor(brand,model){
        this.brand = brand;
        this.modle = model ;
    }

    displayCar(){
       return `${this.brand} ${this.model}`;
    }

const toyotta = new Car("corrolla", "2015");
console.log(toyotta.displayCar());


}